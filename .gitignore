node_modules/
dist/
.DS_Store

# Local-only env overrides (machine-specific). The committed .env.development
# and .env.production are picked up automatically by Vite for `dev` and `build`.
.env
.env.local
.env.*.local
